{
  "metadata": {
    "name": "{{ extra_log_collector.name }}",
    "displayName": "{{ extra_log_collector.name }}",
    "description": "Generated by CMP"
  },
  "desiredState": {
    "integrationRef": {
      "ref": "/platform/integrations/{{ extra_log_collector.name }}"
    },
    "streams": [
      {
        "outputFormat": "SPLUNK",
        "inputDataType": "EVENTS",
        "selector": "filter=(category='security violation')"
      }
    ]
  }
}