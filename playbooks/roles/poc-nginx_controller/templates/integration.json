{
  "metadata": {
    "name": "{{ extra_log_collector.name }}",
    "displayName": "{{ extra_log_collector.name }}",
    "description": "Generated by CMP"
  },
  "desiredState": {
    "type": "GENERIC_INTEGRATION",
    "endpointUri": "{{ extra_log_collector.endpointUri }}",
    "credential": {
      "type": "API_KEY",
      "apiKey": "{{ extra_log_collector.api_key }}"
    }
  }
}